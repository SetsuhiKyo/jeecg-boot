<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.yy.place.mapper.YyPlaceDistanceMapper">

    <select id="getResultByFromTo" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM  yy_place_distance
        WHERE
            start_place = #{fromPlaceId}
            and end_place = #{toPlaceId}
    </select>
    <select id="getDistanceByFromTo" resultType="java.lang.Integer">
        SELECT distance
        FROM  yy_place_distance
        WHERE
            start_place = #{fromPlaceId}
          and end_place = #{toPlaceId}
    </select>
</mapper>